//This field should include the .json file extension.
let fileName = ''

let messengerData = require(`./${fileName}`);

//Logs the data from the .json file.
console.log(messengerData)

//This block parses the JSON object 